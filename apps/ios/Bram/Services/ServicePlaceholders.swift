import Foundation

enum ServicePlaceholders {
    static let supabase = "Supabase auth and sync will be added after the local note loop."
    static let revenueCat = "RevenueCat will own subscription entitlement state."
    static let postHog = "PostHog events must not include raw workout note bodies."
    static let openAI = "OpenAI calls should use pseudonymous IDs and concise outputs."
}
